export default (state = [], action) => {
	switch (action.type) {
		default:
			return state;
	}
};

// export default (state = [], action) => {
// 	switch (action.type) {
// 		case users:
// 			return [ ...state, action ];

// 		default:
// 			return state;
// 	}
// };
